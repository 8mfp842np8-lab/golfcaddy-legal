{
  "mcpServers": {
    "kickbase": {
      "command": "node",
      "args": ["${KICKBASE_MCP_SERVER:-vendor/kickbase-api-mcp/mcp-server/dist/index.js}"],
      "env": {
        "KICKBASE_EMAIL": "${KICKBASE_EMAIL:-}",
        "KICKBASE_PASSWORD": "${KICKBASE_PASSWORD:-}"
      }
    }
  }
}
